[project]
name = "wensum-drought-analysis"
version = "0.1.0"
description = "Threshold-based low-flow drought analysis for the River Wensum at Fakenham"
requires-python = ">=3.11"
dependencies = [
  "matplotlib>=3.8",
]

[tool.uv]
package = false

